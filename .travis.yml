services:
  - mysql
matrix:
  include:
    - language: rust
      rust: nightly
      before_script:
        - cd backend
        - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'
      script:
        - RUST_BACKTRACE=full cargo test
